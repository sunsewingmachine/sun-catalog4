/**
 * Catalog page: loads products (version check + cache or fetch) and renders CatalogLayout.
 */

import CatalogPageClient from "./CatalogPageClient";

export default function CatalogPage() {
  return <CatalogPageClient />;
}
